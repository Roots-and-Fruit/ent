# Testing

## Fixture

Sibling workspace: `C:\Users\reach\OneDrive\Ent-workspace-test`

```
Ent-workspace-test/
├── ent/          ← clone of Ent repo
├── .env          ← local credentials (live gates only)
├── .ent/
├── content/
└── .cursor/      ← generated by ent sync
```

## Phase gates

### Phase 1

```bash
cd C:\Users\reach\OneDrive\Ent
npm install
node tools/ent.mjs validate-manifest
```

Branding boundary:

```bash
node tools/ent.mjs test branding-boundary
```

Kit runtime boundary — zero `.ps1` under `tools/`:

```bash
node tools/ent.mjs test kit-runtime-boundary
```
