#!/usr/bin/env node
/**
 * Stub MCP launcher — Phase 5 implements full transport.
 * Exits 1 with clear message when WP_MCP_* env is missing.
 */
import process from "node:process";

console.error("run-wordpress-mcp.mjs: stub — configure WP_MCP_* in workspace .env (Phase 5)");
process.exit(1);
